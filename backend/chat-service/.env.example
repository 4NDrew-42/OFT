# ORION-CORE Chat Backend Configuration

# Server Configuration
PORT=3002
NODE_ENV=production

# Database Configuration (ORION-MEM PostgreSQL)
DB_HOST=192.168.50.79
DB_PORT=5432
DB_NAME=orion_core
DB_USER=orion
DB_PASSWORD=changeme

# CORS Configuration (automatically handled in server.js)
# Allowed origins: localhost:3000, localhost:3005, *.sidekickportal.com, *.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=200
