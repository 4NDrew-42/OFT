# ORION-CORE Chat Backend Configuration

# Server Configuration
PORT=3002
NODE_ENV=production

# Database Configuration (ORION-MEM PostgreSQL via PgBouncer)
DB_HOST=192.168.50.79
DB_PORT=6432
DB_NAME=orion_core
DB_USER=orion_user
DB_PASSWORD=changeme
# PHASE 1 SECURITY: Enable SSL for database connections (recommended on LAN)
DB_SSL=false

# PHASE 1 SECURITY: Single-User Authorization
# CRITICAL: Only this email can access session APIs
# Must match the authenticated user's email exactly (case-insensitive)
AUTHORIZED_USER_EMAIL=jamesandrewklein@gmail.com

# PHASE 1 SECURITY: JWT Verification (HS256)
# CRITICAL: Must match frontend ORION_SHARED_JWT_SECRET exactly
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ORION_SHARED_JWT_SECRET=your_64_byte_hex_secret_here
ORION_SHARED_JWT_AUD=orion-core
ORION_SHARED_JWT_ISS=https://www.sidekickportal.com

# PHASE 1 SECURITY: CORS Configuration (strict, no wildcards)
# Comma-separated list of allowed origins (no trailing slashes)
ALLOWED_ORIGINS=https://www.sidekickportal.com,https://sidekickportal.com,http://localhost:3000,http://localhost:3005

# PHASE 1 SECURITY: Rate Limiting (configurable)
# Default: 100 requests per 15 minutes per IP
RATE_LIMIT_MAX_REQUESTS=100
