{
  "name": "@orion-core/chat-backend",
  "version": "1.0.0",
  "description": "ORION-CORE Chat Backend Service - Session management and message persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node -e \"const pool = require('./db'); const fs = require('fs'); const sql = fs.readFileSync('./migrations/001_create_chat_tables.sql', 'utf8'); pool.query(sql).then(() => { console.log('✓ Migration complete'); process.exit(0); }).catch(err => { console.error('Migration failed:', err); process.exit(1); });\""
  },
  "keywords": [
    "orion-core",
    "chat",
    "sessions",
    "postgresql",
    "express"
  ],
  "author": "ORION-CORE Team",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
